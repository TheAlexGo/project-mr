.layout-main
    position relative
    display flex
    flex-grow 1
    flex-direction column
    width 100%
    padding $offset-4x $offset-4x $size-nav
    overflow-y auto

.navbar
    position fixed
    bottom env(safe-area-inset-bottom)
    left 0
    z-index 10
