.wrapper {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 16px;

    useMediaWidth();
}

.navbar {
    width: 100%;
    height: $size-nav;
    padding: 4px 0;
    background-color: $color_surface;
}
