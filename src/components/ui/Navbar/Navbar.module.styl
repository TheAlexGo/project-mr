.wrapper {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 16px;

    useDesktopWidth();
}

.navbar {
    width: 100%;
    height: $size-nav;
    padding: 4px 0;
    background-color: var(--color_surface);
}
